import 'package:flutter/material.dart';

class AddressManager extends StatelessWidget {
  const AddressManager({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
